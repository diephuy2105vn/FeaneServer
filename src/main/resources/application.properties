spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ct466_project}
spring.datasource.username=${DB_USERNAME:tuanhuy}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.security.user.password=Test12345_
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

# App Properties
spring.app.jwtCookieName=${JWT_COOKIENAME:springboot}
spring.app.jwtSecret=${JWT_SECRET:======================BezKoder=Spring===========================}
spring.app.jwtExpirationMs=${JWT_EXPIRATIONMS:86400000}